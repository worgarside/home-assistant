---
alias: /input-number/prusa-i3-mk3/tool-0-target/set

id: input_number_prusa_i3_mk3_tool_0_target_set

mode: queued

trigger:
  - platform: state
    entity_id: input_number.prusa_i3_mk3_target_hotend_temperature
    to:

variables:
  payload:
    value: "{{ trigger.to_state.state | int(0) }}"

action:
  - service: mqtt.publish
    data_template:
      topic: "/octoprint/prusa_i3_mk3/tool/0/target/set"
      payload: "{{ payload }}"
