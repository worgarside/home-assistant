---
alias: /homeassistant/reload-all/mobile-notification-actioned

id: homeassistant_reload_all_mobile_notification_actioned

mode: single

trigger:
  - platform: event
    event_type: mobile_app_notification_action
    event_data:
      action: CALL_SERVICE:homeassistant.reload_all

action:
  - service: script.debug_persistent_notification
    data:
      title: Reload Requested
      message: |
        ```
        {{ trigger | tojson(indent=2) }}
        ```

  - service: homeassistant.reload_all
