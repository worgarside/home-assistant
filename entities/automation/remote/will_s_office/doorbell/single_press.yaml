---
alias: /remote/will-s-office/doorbell/single-press

id: remote_will_s_office_doorbell_single_press

mode: single

max_exceeded: silent

trigger:
  - domain: mqtt
    platform: device
    device_id: 5c80508cb067d126c77d8a368cd3507a
    type: action
    subtype: single

action:
  - service: script.mtrxpi_queue_content
    data:
      id: gif-alert-bell
      priority: "{{ states('input_number.mtrxpi_gif_alert_bell_queue_position') }}"
