---
force_update: true

icon: mdi:water-percent

name: FlmtPi Ambient Humidity

state_class: measurement

state_topic: /homeassistant/flmtpi/dht22

unique_id: flmtpi_ambient_humidity

unit_of_measurement: "%"

value_template: "{{ iif(0 < value_json.humidity | int(-999) < 100, value_json.humidity, None) }}"
