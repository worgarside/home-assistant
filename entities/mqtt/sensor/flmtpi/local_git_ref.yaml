---
force_update: false

icon: mdi:source-repository

name: FlmtPi Local Git Ref

state_topic: /homeassistant/flmtpi/stats

unique_id: flmtpi_local_git_ref

value_template: "{{ value_json.local_git_ref }}"
