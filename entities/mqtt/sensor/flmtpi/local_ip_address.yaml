---
force_update: true

icon: mdi:ip-network-outline

name: FlmtPi Local IP Address

state_topic: /homeassistant/flmtpi/stats

unique_id: flmtpi_local_ip_address

value_template: "{{ value_json.local_ip }}"
