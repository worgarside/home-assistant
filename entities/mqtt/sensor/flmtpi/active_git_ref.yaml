---
force_update: false

icon: mdi:source-branch-sync

name: FlmtPi Active Git Ref

state_topic: /homeassistant/flmtpi/stats

unique_id: flmtpi_active_git_ref

value_template: "{{ value_json.active_git_ref }}"
