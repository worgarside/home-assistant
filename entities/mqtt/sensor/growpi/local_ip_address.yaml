---
force_update: true

icon: mdi:ip-network-outline

name: GrowPi Local IP Address

state_topic: /homeassistant/growpi/stats

unique_id: growpi_local_ip_address

value_template: "{{ value_json.local_ip }}"
