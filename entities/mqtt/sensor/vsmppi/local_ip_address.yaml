---
force_update: true

icon: mdi:ip-network-outline

name: VSMPPi Local IP Address

state_topic: /homeassistant/vsmppi/stats

unique_id: vsmppi_local_ip_address

value_template: "{{ value_json.local_ip | default('vsmppi.local') }}"
