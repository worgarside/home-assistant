---
force_update: false

icon: mdi:source-repository

name: VSMPPi Local Git Ref

state_topic: /homeassistant/vsmppi/stats

unique_id: vsmppi_local_git_ref

value_template: "{{ value_json.local_git_ref }}"
