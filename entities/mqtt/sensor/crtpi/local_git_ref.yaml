---
force_update: false

icon: mdi:source-repository

name: CRTPi Local Git Ref

state_topic: /homeassistant/crtpi/stats

unique_id: crtpi_local_git_ref

value_template: "{{ value_json.local_git_ref }}"
