---
force_update: true

icon: mdi:ip-network-outline

name: CrtPi Local IP Address

state_topic: /homeassistant/crt-pi/stats

unique_id: crtpi_local_ip_address

value_template: "{{ value_json.local_ip | default('crtpi.local') }}"
