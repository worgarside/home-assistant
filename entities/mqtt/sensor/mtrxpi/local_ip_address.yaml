---
force_update: true

icon: mdi:ip-network-outline

name: MtrxPi Local IP Address

state_topic: /homeassistant/mtrxpi/stats

unique_id: mtrxpi_local_ip_address

value_template: "{{ value_json.local_ip | default('mtrxpi.local') }}"
