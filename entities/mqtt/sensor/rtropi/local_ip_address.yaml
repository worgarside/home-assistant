---
force_update: true

icon: mdi:ip-network-outline

name: RtroPi Local IP Address

state_topic: /homeassistant/rtropi/stats

unique_id: rtropi_local_ip_address

value_template: "{{ value_json.local_ip | default('rtropi.local') }}"
