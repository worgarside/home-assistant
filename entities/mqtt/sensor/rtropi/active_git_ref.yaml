---
force_update: false

icon: mdi:source-branch-sync

name: RtroPi Active Git Ref

state_topic: /homeassistant/rtropi/stats

unique_id: rtropi_active_git_ref

value_template: "{{ value_json.active_git_ref }}"
