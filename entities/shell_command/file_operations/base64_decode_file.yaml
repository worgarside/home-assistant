---
base64_decode_file: >-
  {% set file = file.strip('/"') %}
  {% set output = iif(output is string, output, "{}.decoded".format(file)) %}
  base64 -d 'resources/tmp/{{ file }}' > 'resources/tmp/{{ output }}'
