---
base64_decode_file: >-
  {% set file = file.strip('/"') %}
  {% set output = iif(output is string, output, "{}.encoded".format(file)) %}
  base64 -w 0 'resources/tmp/{{ file }}' > 'resources/tmp/{{ output }}'
