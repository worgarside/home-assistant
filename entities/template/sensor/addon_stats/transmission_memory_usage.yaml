---
icon: mdi:memory

name: Transmission Memory Usage

state: "{{ state_attr('sensor.transmission_status', 'data').memory_percent | default(0) }}"

unique_id: transmission_memory_usage

unit_of_measurement: "%"
