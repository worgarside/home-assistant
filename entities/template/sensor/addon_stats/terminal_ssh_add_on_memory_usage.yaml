---
icon: mdi:memory
name: Terminal & SSH Add-on Memory Usage
state: >
  {{
    state_attr('sensor.terminal___ssh_add_on_status', 'memory_percent') |
    float(default=0) * 100 |
    round(2)
  }}
unique_id: terminal_ssh_add_on_memory_usage
unit_of_measurement: '%'
