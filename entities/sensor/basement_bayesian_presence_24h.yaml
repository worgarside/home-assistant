---
# https://www.home-assistant.io/integrations/history_stats/
platform: history_stats

name: Basement Bayesian Presence 24h

unique_id: basement_bayesian_presence_24h

entity_id: binary_sensor.basement_bayesian_presence

state: "on"

type: ratio

start: "{{ now() - timedelta(hours=24) }}"

end: "{{ now() }}"
