---
sensor:
  command: cd /config && git describe --tags --exact-match 2>/dev/null || git rev-parse --short HEAD
  name: Current Git Ref
  scan_interval: 120
  unique_id: current_git_ref
  icon: mdi:git
