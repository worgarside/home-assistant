---
sensor:
  command: !secret addon_stats_command_sponsorblockcast

  json_attributes:
    - data

  name: CastSponsorSkip Status

  scan_interval: 60

  unique_id: castsponsorskip_status

  value_template: "{{ value_json.result }}"
