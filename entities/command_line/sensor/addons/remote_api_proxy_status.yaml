---
sensor:
  command: !secret addon_stats_command_remote_api
  json_attributes:
    - data
  name: Remote API Proxy Status
  scan_interval: 60
  unique_id: remote_api_proxy_status
  value_template: "{{ value_json.result }}"
