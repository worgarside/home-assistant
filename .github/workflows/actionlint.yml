---
name: actionlint

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

jobs:
  actionlint:
    name: Lint Workflows
    uses: worgarside/github-config-files/.github/workflows/__actionlint.yml@main
