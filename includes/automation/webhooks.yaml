---

# ------------------------ Webhooks ------------------------ #

  - alias: /oauth/get_auth_code
    id: get-oauth-exchange-code
    trigger:
      - platform: webhook
        webhook_id: get-oauth-exchange-code
    action:
      - service: persistent_notification.create
        data_template:
          title: "OAuth2 Code Received"
          message: |
            ```json
            {{ trigger.json }}
            ```
